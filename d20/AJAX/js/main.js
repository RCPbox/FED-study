/*! main.js */
(function(global, $, undefined){
	'use strict';

// .toArray()로 제이쿼리 인스턴스를 reverse() 할수도 있다.
// 제이쿼리에는 reverse가 없다. 배열로 바꿧기 때문에 reverse 사용가능.
	// $('*').toArray().reverse().forEach(function(item, index){
	// 	console.log(item, index);
	// });

	console.log( $.cache, $.fn.radioClass );// 라이브러리 호출 확인.

})(window, window.jQuery);


